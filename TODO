- Prefix private methods with '_'
- Reorder methods in classes to have public methods last
- Split templates for overview and full view
- Factor out methods to render overview and full modes in EntryView
- Show date and feed info on each entry
- Implement 'all feeds' feed
- Trigger feed selection in AppView instead of EntriesView
- Fix initial access from client-side route
- Optimize all css selectors with '>' where possible
- Next/previous navigation with keyboard on overview
- Cleanup stylus files to be more params-based
- Change 'u' to update current feed when already in overview
